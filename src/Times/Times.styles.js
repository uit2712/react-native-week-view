import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  columnContainer: {
    paddingTop: 10,
    minWidth: 60,
  },
  label: {
    flex: -1,
  },
  text: {
    fontSize: 12,
    textAlign: 'center',
  },
});

export default styles;
